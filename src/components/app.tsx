import { Counter } from "$/components/counter";

export function App() {

  return (
    <div>
      <h1>Wheel Event tutorial with TSX + Alpine.js</h1>
      <div class="card">
        <Counter />
      </div>
    </div>
  );
}
